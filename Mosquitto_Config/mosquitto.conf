# Mosquitto configuration file
# Listen on all interfaces (0.0.0.0 for IPv4)
listener 1883 0.0.0.0
protocol mqtt

# Optional: Allow anonymous access (useful for testing)
allow_anonymous true

# Optional: Persistence
persistence false
